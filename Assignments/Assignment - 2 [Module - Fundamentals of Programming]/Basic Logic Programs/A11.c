//Find circumference of square formula : C = 4 * a

#include<stdio.h>

main()
{
	float s, peri;
	printf("Enter the side of the Square (in cm) :");
	scanf("%f",&s);
	peri=(4*s);
	printf("The perimeter of the Square is %.2f cm",peri);
}
