//Find circumference of Rectangle formula : C = 4 * a

#include<stdio.h>

main()
{
	float s, circum;
	printf("Enter the side of the Rectangle (in cm) :");
	scanf("%f",&s);
	circum=(4*s);
	printf("The circumference of the Rectangle is %.2f cm",circum);
}
