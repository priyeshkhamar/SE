//Find Area of Square formula : a = a2

#include<stdio.h>

main()
{
	float s, area;
	printf("Enter the length of one side of the square (in cm) :");
	scanf("%f",&s);
	area=s*s;
	printf("The area of the square is %.2f cm^2",area);
}
