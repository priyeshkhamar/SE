// Write a program in C to find the length of a string without using library functions.

#include<stdio.h>
#include<string.h>

main()
{
    char a[100], i, j, length = 0;

    printf("Enter a String : ");
    gets(a);

    for ( i = 0; a[i] != '\0'; i++)
    {
        
    }
    printf("Length of the String : %d",i);
    
}